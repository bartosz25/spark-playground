The code shows potential problems that might be caused by the `.withColumn` method, `CollapseProject` logical optimization
rule and the `TRACE` logging. 

To test them, run the `DataDispatcherIfElseStudy` with one of the following parameters: _v1_, _v2_, _v3_, or _v4_ . Only
the last, _v4_, version should terminate correctly.